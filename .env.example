# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/yusuf_sugiyarto?schema=public"

# Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-at-least-32-random-characters"
FRONTEND_URL="http://localhost:3000"
BACKEND_URL="http://localhost:4000"

# Used to pseudonymize rate-limit identifiers. Do not reuse AUTH_SECRET.
RATE_LIMIT_SALT="replace-with-another-random-secret"

# Seed only; remove from the production runtime after seeding.
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="replace-with-a-strong-password"
